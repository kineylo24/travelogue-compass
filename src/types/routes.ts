export interface SearchResult {
  name: string;
  address: string;
  coordinates: [number, number];
}

export interface Destination {
  name: string;
  address: string;
  coordinates: [number, number];
}
